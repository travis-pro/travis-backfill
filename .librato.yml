spaces:
  -
    name: "Backfill"
    order:
      - schedule_counts.json
      - schedule_cursor.json
      - schedule_page_median_95th.json
      - backfill_query_95ths.json
      - backfill_query_medians.json
      - backfill_queue_size.json
      - backfilling_pull_request_records_count.json

accounts:
  com:
    user: mathias@travis-ci.com
    vars:
      app: travis-com-backfill

  org:
    user: mathias@travis-ci.org
    vars:
      app: travis-org-backfill

